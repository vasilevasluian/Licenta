# Validate User Admin page

## Verify if User Admin page is integral
Tags: smoke
* Login as "SIMPLE_USER"
* Go to "USER_ADMIN" page
* Verify if user admin page is integral

## Verify the showing entries filters
Tags: smoke
* Login as "SIMPLE_USER"
* Go to "USER_ADMIN" page
* Verify if the showing entries filter are displayed

## Verify whether the user can display users by first name/last name/role/email
* Login as "SIMPLE_USER"
* Go to "USER_ADMIN" page
* Check if the user can display users by first name/last name/role/email